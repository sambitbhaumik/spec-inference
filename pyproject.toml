[project]
name = "inference-engine"
version = "0.1.0"
description = "A speculative-decoding inference engine for large language models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate",
    "bitsandbytes",
    "pybind11",
    "pyyaml",
    "rich",
    "torch",
    "transformers",
]

